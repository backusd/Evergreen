{
	// See Button.json for details on how to declare a Button. 
	// All the rules that apply to Button are also valid for RoundedButton

	// In addition, RoundedButton supports two additional attributes for setting the rounded edges
	"TestButton3": {
		"Type": "RoundedButton", // Type must be 'RoundedButton'
		"CornerRadiusX": 15.0, // (optional) Corner radius values. Default will be 0 (non-rounded corners)
		"CornerRadiusY": 25.0 // Identical values will make perfectly circular corners
	}
}